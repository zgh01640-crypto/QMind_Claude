services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: qmind
      POSTGRES_USER: qmind
      POSTGRES_PASSWORD: qmind
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
